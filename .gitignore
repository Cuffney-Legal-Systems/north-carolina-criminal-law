# Developer maintenance scripts — never ship to customers
maintenance/

# Downloaded PDF forms — fetched on demand, not stored in the repo
skills/nc-aoc-cr-forms/pdfs/

# Pattern jury instructions — cached source PDFs (large; reference/ markdown ships instead)
skills/north-carolina-pattern-jury-instructions/cache/

# macOS
.DS_Store

# Python
__pycache__/
*.pyc
*.pyo
*.tmp

# SAM deployment artifacts — build output and deployment config; never commit
mcp-server/samconfig.toml
mcp-server/.aws-sam/

# Claude Code local settings — contains absolute local paths; machine-specific
.claude/settings.local.json

# Legacy paths (pre-v0.3 structure, safe to delete)
skill/
nc_aoc_cr_forms/
zipH84wx
*.skill
